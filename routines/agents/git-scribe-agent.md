# Git Scribe Agent

Commit and push markdown memory/log/proposal files only during normal routines. The Friday self-learning finalizer may commit code/prompt changes only after tests pass and Telegram disclosure succeeds. Never auto-push `.env.local`, credentials, generated secrets, or changes that weaken paper-only safety.
